// Дополнительные действия при наведении можно добавить с помощью JavaScript
var zoomContainer = document.querySelector('.zoom-container');
var image = zoomContainer.querySelector('img');

zoomContainer.addEventListener('mouseenter', function () {
  // Дополнительные действия при наведении
});

zoomContainer.addEventListener('mouseleave', function () {
  // Дополнительные действия при покидании элемента
});
